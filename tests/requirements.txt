# Test requirements for PlexAudioConverter strong test system
# Install with: pip install -r tests/requirements.txt

# Core testing framework
pytest>=7.0.0
pytest-asyncio>=0.21.0  # For async test support
pytest-timeout>=2.1.0   # Test timeouts
pytest-xdist>=3.0.0     # Parallel test execution
pytest-mock>=3.10.0     # Enhanced mocking

# Coverage reporting
coverage[toml]>=7.0.0
pytest-cov>=4.0.0

# Test reporting and CI integration
pytest-html>=3.1.0      # HTML test reports
pytest-json-report>=1.5.0  # JSON test reports
pytest-benchmark>=4.0.0  # Performance benchmarking

# Test utilities
pytest-random-order>=1.1.0  # Randomized test execution
pytest-repeat>=0.9.1     # Repeat tests for flakiness detection
pytest-rerunfailures>=11.0  # Rerun failed tests

# Database testing
pytest-postgresql>=4.1.0  # If needed for SQLite alternatives
factory-boy>=3.2.0       # Test data factories

# Platform-specific testing
pytest-platform>=0.3.0   # Platform markers

# Development and debugging
pytest-pudb>=0.7.0       # Debugger integration
pytest-sugar>=0.9.6      # Better test output formatting
pytest-clarity>=1.0.1    # Better assertion diffs

# Temporary file handling for tests
pytest-tempdir>=2019.10.12

# Monitoring and metrics
pytest-monitor>=1.6.0    # Test performance monitoring

# Optional: For advanced testing scenarios
hypothesis>=6.0.0         # Property-based testing
freezegun>=1.2.0         # Time manipulation for tests
responses>=0.22.0        # HTTP mocking if needed

# Type checking in tests
mypy>=1.0.0              # Type checking
pytest-mypy>=0.10.0     # Mypy integration with pytest

# Documentation testing
pytest-doctestplus>=0.12.0  # Enhanced doctest support